import { FormatarDatasPipe } from './formatar-datas.pipe';

describe('FormatarDatasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarDatasPipe();
    expect(pipe).toBeTruthy();
  });
});
